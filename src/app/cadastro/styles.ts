import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
container: {
    marginTop: 100, 
    display: "flex",
    alignItems: "center"
},
campos: {
    // backgroundColor: "#000",
    // borderColor: "#000"
}
})